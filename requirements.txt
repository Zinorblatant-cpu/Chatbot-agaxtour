# Dependências principais
python-telegram-bot==20.7

# Opcional (mas recomendado)
aiohttp>=3.8.5       # HTTP assíncrono usado internamente pelo bot
httpx>=0.27.0        # útil para chamadas externas se o bot consumir APIs
python-dotenv>=1.0.1 # se quiser carregar o TOKEN de um arquivo .env
